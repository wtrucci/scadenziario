"""
Scadenziario — gestione dei servizi a scadenza dei clienti.

``__version__`` is the single source of truth for the version shown in the web
interface and reported by the API docs. It must match the released git tag:
bump it in the same commit you tag (see CLAUDE.md, "Rilascio").
"""

__version__ = "0.8.3"
